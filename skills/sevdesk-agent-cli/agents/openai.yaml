display_name: "sevdesk Agent CLI"
short_description: "Read-first sevdesk CLI with guarded writes and context snapshot support"
default_prompt: "Use sevdesk-agent commands to inspect sevdesk safely, execute guarded writes only when explicitly confirmed, and generate a context snapshot for handoff."
