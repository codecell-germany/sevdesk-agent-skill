display_name: "sevdesk Agent CLI"
short_description: "Read-first sevdesk CLI with guarded writes, safe PDF export, verification, and context snapshots"
default_prompt: "Use sevdesk-agent in read-first mode, prefer find-contact for contact discovery, keep PDF export in safe mode (preventSendBy=1 default), use guarded writes with --verify for createContact/createOrder, and finish with a context snapshot for handoff."
